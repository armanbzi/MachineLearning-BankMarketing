# ML Lab Project — Bank Marketing (UPC-FIB, MDS 2026)
# Python 3.13 (tested on 3.13.9). Install with:  pip install -r requirements.txt
# Versions pinned to those used to produce the committed notebook outputs, so results reproduce.

numpy==2.3.5
pandas==2.3.3
scikit-learn==1.8.0
scipy==1.16.3
matplotlib==3.10.6
seaborn==0.13.2
shap==0.52.0            # TreeSHAP feature importance (Phase IV)
requests==2.32.5        # downloads the dataset in preprocessing.ipynb

# Tooling to run the notebooks
jupyter==1.1.1
nbformat==5.10.4
